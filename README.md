# Sort-By-New-For-Aliexpress
Adds the sort by new button back to aliexpress


# Known bugs

Sorting resets when changing pages, this can be worked around by manually editing the URL to increase/decrease the page number.
